.container
  .row
    h2= t('.title')
  table.table
    thead
      tr
        th= ta(User, :email)
        th= ta(User, :gender)
        th= ta(User, :age)
        th= ta(User, :interests)
        th= t('common.edit')
    tbody
      - users.each do |user|
        tr
          td= user.email
          td= user.gender
          td= user.age
          td= user.pretty_interests
          td= link_to t('common.edit'), [:edit, :admin, user]
  .row
    .col-md-2
      = link_to t('common.add'), [:new, :admin, :user], class: 'btn btn-primary'
